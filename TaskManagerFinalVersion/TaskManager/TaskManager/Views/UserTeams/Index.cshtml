@model IEnumerable<TaskManager.DataAccess.DataModels.UserTeams>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout_After_Login.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Job)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Team)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Job)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Team.TeamsId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserTeamsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserTeamsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserTeamsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
